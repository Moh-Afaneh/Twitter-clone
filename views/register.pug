extends layouts/login_layout.pug
block content
  .loginContainer#loginContainer
    .title-wrapper  
      p.logo.fas.fa-dove 
      h1.title-h1 Register to Twitter
      .alert.alert-danger.d-none#alertMessage(role='alert')
        This the passwords Do not match Try again!!
    form#registerForm(method="post" onsubmit="event.preventDefault(); validateForm();") 
      p.errorMessage #{errorMessage}
      input#firstname(type="text", name="firstname" , placeholder="firstname" , required="" , value=firstname )
      input#lastname(type="text", name="lastname" , placeholder="lastname" , required="" . value=lastname )
      input#username(type="text", name="username" , placeholder="username" , required="" ,value=username )
      input#email(type="email", name="email" , placeholder="Email" , required="" , value=email )
      input#password(type="password", name="password" , placeholder="password" , required="")
      input#passwordConf(type="password", name="passwordConf" , placeholder="Confirm password" , required="")
      input(type="submit", value="Create a account")
    a(href="/login") already have an account ? login now 
  script.
     const passwordField= document.getElementById("password");
     const passwordConfirmField= document.getElementById("passwordConf");
     const registerForm= document.getElementById("registerForm");
     const alertMessage= document.getElementById("alertMessage");
     function validateForm() {
      if(passwordField.value !== passwordConfirmField.value) {
       alertMessage.classList.remove("d-none")
      } else {
         registerForm.submit();
      }
     }